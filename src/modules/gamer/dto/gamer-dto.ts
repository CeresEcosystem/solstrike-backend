// Legacy field names to support DEO Arena game
export interface GamerDto {
  username: string;
  account_id: string;
  referral_code: string;
  referral_used: boolean;
  referral_user_id: number;
  reservedChips: number;
  points: number;
  party: number;
}
